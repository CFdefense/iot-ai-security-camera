# Copy to `.env` (or export in your shell) before running the services.

# REST API configuration
# Bind to LAN interface only, NOT 0.0.0.0 (see Architecture section 7).
API_HOST=127.0.0.1

# Use 5050 locally on macOS unless you disabled AirPlay Receiver (port 5000 conflicts).
API_PORT=5050

# Pre-shared key: required for REST guards and for the web dashboard (/login).
API_KEY=change-me-to-a-long-random-string

# Web dashboard (/login, /dashboard) — seeded into SQLite dashboard_users via db.inject_dashboard_credentials_from_env
SESSION_SECRET=change-this-to-a-long-random-string-for-flask-sessions
USER_EMAIL=email@change-me.com
USER_PASSWORD=password-change-me

# MQTT configuration
MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_CLIENT_ID=pi-security-camera
HEARTBEAT_INTERVAL_SEC=60

# Detection configuration
SENSOR_ID=front_door_cam

# Cosine similarity gate for SFace embeddings (tune accordingly - to test)
MATCH_THRESHOLD=0.6
# Optional: if Arduino JSON adds class + score (object/label + score/confidence), skip embedding for non-person
# or weak person confidence.
# PERSON_DETECTION_MIN_SCORE=0.5

# Dashboard BLOBs: registration photos + detection alert images (same max edge and JPEG quality)
# STORED_JPEG_MAX_EDGE=640
# STORED_JPEG_QUALITY=85

# Optional: directory with face_detection_yunet_2023mar.onnx + face_recognition_sface_2021dec.onnx (default: src/camera/picam/models/)
# FACE_MODEL_DIR=

# Serial (Arduino) for the serial bridge. USB Arduino: /dev/ttyACM0. Fake emitter: use the path
# printed by `uv run fake-sensor` (e.g. SERIAL_PORT=/dev/pts/4), not ttyACM0.
SERIAL_PORT=/dev/ttyACM0

# Storage configuration
CAMERA_DB_PATH=./src/data/whitelist.sqlite

# Logging configuration
# TASK = concise operational milestones (recommended default)
# INFO = more verbose app logs
# DEBUG = deep troubleshooting output
LOG_LEVEL=TASK

# Native libcamera C++ logging verbosity (reduces camera_manager/pisp spam in normal runs)
# LIBCAMERA_LOG_LEVELS=*:ERROR